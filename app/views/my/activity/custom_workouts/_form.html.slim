= render partial: 'shared/validation_errors', locals: { model: custom_workout }

= form_for [:my, custom_workout] do |f|
  .form-group.row
    = f.label :title, class: 'col-4 col-lg-3 col-form-label'
    .col.col-lg-8
      = f.text_field :title, class: 'form-control', required: 'required', max: 100
      small.form-text.text-muted
        | (100 characters maximum)

  = render partial: 'shared/notes', locals: { form: f }
  = render partial: 'shared/feeling', locals: { form: f, value: custom_workout.feeling }

  .form-group.row
    .col-4.col-lg-3
    .col-8
      = f.submit t('shared.buttons.save'), class: 'btn btn-success'

